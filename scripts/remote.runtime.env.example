# 这份文件放在远端服务器上，由 publish_to_remote.sh 在 docker run 前读取。

DOCKER_BIN=docker
CONTAINER_RESTART=unless-stopped

# 使用 | 分隔多个映射、挂载或参数。
CONTAINER_PORTS=15080:8080|64000-65000:64000-65000
CONTAINER_VOLUMES=/apps/proxytransfer/data:/app/App_Data|/apps/proxytransfer/config/appsettings.Production.json:/app/appsettings.Production.json:ro
CONTAINER_ENVS=ASPNETCORE_ENVIRONMENT=Production|TunnelHost__ApiUrl=http://0.0.0.0:8080

# 可选。
# CONTAINER_NETWORK=bridge
# CONTAINER_EXTRA_ARGS=--log-opt=max-size=10m|--log-opt=max-file=3
# REMOTE_DOCKER_CONTEXT=/opt/proxytransfer/app
# SSH_EXTRA_ARGS=-o|StrictHostKeyChecking=no
# REMOTE_POST_DEPLOY=docker ps --filter name=proxytransfer-api